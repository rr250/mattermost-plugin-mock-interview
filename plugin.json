{
    "id": "com.rr250.mock-interview-plugin",
    "name": "Mock Interview",
    "description": "Create mock interviews",
    "version": "0.0.1",
    "server": {
      "executables": {
          "linux-amd64": "server/dist/plugin-linux-amd64",
          "darwin-amd64": "server/dist/plugin-darwin-amd64",
          "windows-amd64": "server/dist/plugin-windows-amd64.exe"
      }
    },
    "settings_schema": {
      "settings": [{
        "key": "ChannelID",
        "display_name": "Channel ID",
        "type": "text",
        "help_text": "Channel ID where all mock interview requests will be posted"
      }]
    }
}
